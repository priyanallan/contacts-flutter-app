import 'package:flutter/material.dart';

void main(){
  runApp(
    MaterialApp(
      title: "Flutter App",
      theme: ThemeData(
        primarySwatch: Colors.blue,
        primaryColor: Colors.lightGreen
      ),
    )
  );
}